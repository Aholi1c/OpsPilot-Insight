# 评审对照检查表

> 用途：按赛事评审 5 大维度 + 个性化核验点逐项自查，答辩时可按"评审要求 → 本项目实现 → 证据位置"快速应答。
> 所有证据均可离线验证，路径以 `opspilot-insight/` 为根目录。

## 一、场景价值与行业可复制性（25%）

| 评审要求 | 本项目对应实现 | 证据位置 |
| --- | --- | --- |
| 场景真实、有业务价值 | 故障自愈是 SRE/AIOps 核心场景：告警→根因→处置→验证全流程自动化，直接降低 MTTR 与值班人力 | `README.md`（项目定位）、`examples/scenarios/` 3 个真实故障模式（连接池耗尽/容器 OOM/网络延迟） |
| 场景可复制到其他行业 | MCP 适配器契约隔离业务系统，替换适配器即可迁移；知识库为标准 JSONL 格式可导入企业自有数据 | `src/opspilot/mcp/mock_adapters.py`（适配器接口）、`data/knowledge/*.jsonl` |
| 方案完整覆盖场景闭环 | 告警聚合→根因→方案→安全执行→验证复盘→案例沉淀，六环节无人工断点（中高风险审批为有意设计的人工卡点） | `src/opspilot/orchestrator.py`、运行 `python3 run_demo.py --scenario db_pool_exhaustion --auto-approve` |

## 二、多 Agent 协同与自主闭环（25%）

| 评审要求 | 本项目对应实现 | 证据位置 |
| --- | --- | --- |
| 多 Agent 明确分工 | 5 个职能 Agent：AlertAgent/RcaAgent/PlannerAgent/ExecutorAgent/VerifierAgent，角色职责声明式配置 | `src/opspilot/agents/`（5 个 Agent 实现）、`config/agents.yaml`、`docs/AGENT_IDENTITY.md` |
| Agent 间有效协同 | 五段串行流水线，Pydantic 强类型消息（Incident/Plan/Execution/Verification）逐段传递，无自由文本黑盒 | `src/opspilot/models.py`、`src/opspilot/orchestrator.py` |
| 自主闭环、容错降级 | 任一阶段失败记录降级说明并继续产出部分报告；执行失败自动回滚后续行备选动作，验证复盘照常完成 | `src/opspilot/orchestrator.py`（分段降级）、运行 `python3 run_demo.py --scenario network_latency --auto-approve` |
| 经验闭环（自主进化） | VerifierAgent 复盘后自动沉淀新案例写回知识库（幂等），供后续 RcaAgent/PlannerAgent 检索 | `src/opspilot/agents/verifier_agent.py`、`data/knowledge/cases.jsonl`、`tests/test_stage2.py`（案例沉淀用例） |

## 三、Skill 工程体系与生态复用（25%）

| 评审要求（个性化核验必选项） | 本项目对应实现 | 证据位置 |
| --- | --- | --- |
| 输入/输出定义 | 每个 Skill 定义输入输出 Schema，基类统一校验 | `src/opspilot/skills/base.py`、`docs/SKILL_CATALOG.md` |
| 调用条件 | 每个 Skill 声明触发条件与适用阶段 | `docs/SKILL_CATALOG.md`（v2.0，逐 Skill 列出） |
| 依赖工具 | 逐 Skill 标注依赖的 MCP 适配器 / RAG / LLM Provider | `docs/SKILL_CATALOG.md`、各 Skill 源码（`src/opspilot/skills/*.py`） |
| 失败处理 | 基类异常捕获 + 各 Skill 失败策略（降级/回滚/跳过），SafeExecute 含检查点回滚 | `src/opspilot/skills/base.py`、`src/opspilot/skills/safe_execute.py` |
| 验证方式 | 每个 Skill 有对应测试用例；RecoveryVerify 提供指标级验证 | `tests/test_stage2.py`、`tests/test_e2e.py` |
| 复用价值 | 9 个 Skill 与场景解耦，Schema 化接口可编入其他 Agent 团队 | `docs/SKILL_CATALOG.md`（复用价值字段） |
| 版本演进 | Skill 清单带版本号管理（当前 v2.0），版本随阶段演进记录 | `docs/SKILL_CATALOG.md`、`README.md`（阶段规划一节） |
| Skill 数量与覆盖 | 9 个：AlertFusion/ImpactMapping/LogTraceRca/CaseRetrieval/RunbookRag/SafeExecute/RiskGuard/RecoveryVerify/Postmortem | `src/opspilot/skills/` 目录（逐文件对应） |

## 四、工程落地、运行验证、安全可审计（20%）

| 评审要求 | 本项目对应实现 | 证据位置 |
| --- | --- | --- |
| 可运行、可复现（红线） | 无 API Key、无网络离线完整复现 3 场景；主流程依赖仅 pydantic | `README.md` 快速开始；命令：`python3 run_demo.py --scenario <场景> --auto-approve` |
| 测试验证 | pytest 39 用例全通过，覆盖闭环/安全/RAG/评测/成本 | `tests/`（test_e2e / test_stage2 / test_stage3）；命令：`python3 -m pytest tests/ -v` |
| 高风险动作审批（核验点） | medium/high 风险方案必须人工审批（交互式 y/n），审批人/时间/决定/理由入审计 | `src/opspilot/agents/executor_agent.py`、运行 `python3 run_demo.py --scenario db_pool_exhaustion`（不加 --auto-approve 进入交互审批） |
| 高风险动作回滚（核验点） | 动作执行前记录检查点，失败逆序自动回滚，续行备选动作 | `src/opspilot/skills/safe_execute.py`；演示：network_latency 场景（首动作失败→回滚→切流成功） |
| 高风险动作审计（核验点） | 审计事件流全落盘：whitelist_check→approval→checkpoint→execute→rollback，均带 trace_id | `src/opspilot/observability/audit.py`、运行后的 `output/audit_*.jsonl` |
| 动作安全边界 | 白名单 7 类动作，白名单外一律拒绝；幂等键防重复执行 | `config/action_whitelist.yaml`、`src/opspilot/skills/safe_execute.py` |
| 全链路可观测 | 自研 Tracer（OTel 语义，22 span/次）+ JSONL 日志 + 指标，全部 trace_id 关联 | `src/opspilot/observability/`、运行后的 `output/trace_*.json` / `run_*.log` / `metrics_*.json` |
| 质量回归手段 | 评测引擎：Golden Dataset 自动构建 + 五维规则评估 + LLM-as-Judge + 一键回放（3 场景 100 分） | `src/opspilot/evaluation/`、`data/golden/golden_dataset.jsonl`；命令：`python3 scripts/replay_eval.py` |
| 成本可控 | Token 成本三维分解（Agent/Skill/Model）+ 预算超限 budget_alert 审计告警 | `config/pricing.yaml`、`src/opspilot/evaluation/cost.py`、`output/metrics_*.json` 的 cost 段 |
| 结果可视化 | Streamlit 四页看板：总览/Trace 浏览器/评测报告/成本分析 | `ui/`；命令：`streamlit run ui/dashboard.py`；截图 `../../page1_home.png` 等 4 张 |

## 五、开放/开源贡献（5%）

| 评审要求 | 本项目对应实现 | 证据位置 |
| --- | --- | --- |
| 开源意愿与规划 | 计划以 Apache 2.0 开源；可观测/评测/成本组件零重依赖，可拆分独立包优先开源 | `docs/submission/方案PPT大纲.md` P13（落地计划与开源规划） |
| 生态可复用产出 | Skill 编写规范（SKILL_CATALOG 模板）、MCP 适配器契约、Golden Dataset 构建方法可供社区复用 | `docs/SKILL_CATALOG.md`、`src/opspilot/mcp/`、`src/opspilot/evaluation/dataset_builder.py` |

## 六、AgentTeams 架构能力映射（个性化核验点）

| AgentTeams 能力 | 本项目实现 | 证据位置 |
| --- | --- | --- |
| 角色编排 | 5 个 Agent 的角色、职责、提示词模板、可用 Skill 声明式配置 | `config/agents.yaml`、`docs/AGENT_IDENTITY.md` |
| 任务拆解 | 故障自愈拆解为五段子任务；Agent 内再拆解为 Skill 调用链 | `src/opspilot/orchestrator.py`、`src/opspilot/agents/*.py` |
| 上下文传递 | Pydantic 强类型消息逐段传递（Incident/Plan/Execution/Verification） | `src/opspilot/models.py` |
| 协同执行 | 五段流水线 + 分段降级 + 失败回滚后续段照常执行 | `src/opspilot/orchestrator.py`、`tests/test_e2e.py` |
| 状态追踪 | 全链路 Trace span 树 + JSONL 日志 + 审计事件 + 指标，trace_id 全关联 | `src/opspilot/observability/tracer.py`、`output/trace_*.json`、看板 Trace 浏览器页 |

## 七、答辩前自查清单

- [ ] 本地执行 `python3 -m pytest tests/ -v`，确认 39 用例全绿并截图
- [ ] 本地执行 `python3 run_demo.py --scenario network_latency --auto-approve`，确认回滚剧本输出并截图
- [ ] 本地执行 `python3 scripts/replay_eval.py`，确认 3 场景评分并截图
- [ ] 启动看板 `streamlit run ui/dashboard.py`，四页面各截一张最新图
- [ ] 检查 `output/` 内五类产物文件齐全（incident_report/trace/run.log/audit/metrics）
- [ ] 演练交互式审批：不加 `--auto-approve` 运行一次，展示 y/n 审批与审计记录
- [ ] PPT 中所有数字（5 Agent / 9 Skill / 39 用例 / 22 span / 13 Runbook / 11 案例 / 3 场景 100 分）与代码实况一致
